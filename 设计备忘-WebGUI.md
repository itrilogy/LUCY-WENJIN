# Web GUI 设计备忘

## 背景

现有 Qt 桌面应用（`qt/topschool/`）存在分发困难的问题——需要编译、安装 Qt 运行时，每人一套环境。计划改造成 Web 应用，部署在已有 VPS（1Panel）上，供同班同学使用。

## 目标

1. **替换 Qt 桌面应用**，用浏览器即可查询
2. **覆盖现有 4 个 Tab 页功能** + 新增定向查询/志愿表功能
3. **部署在 VPS**，1Panel 管理，公网可访问
4. **长期维护**，每年更新数据

## 技术方案

```
Flask + SQLite + Jinja2 模板
部署: 1Panel → Python 项目 → Nginx 反向代理
数据库: 直接复用现有 gaokao2025.sqlite，零迁移
```

### 为什么选 Flask 而非 CF D1

| 方案 | 结论 |
|------|------|
| **VPS + Flask** | 已有 VPS，SQLite 直接上传，最快落地 |
| Cloudflare D1 | 需要写迁移脚本导入数据，BLOB 字段兼容性差 |
| Railway | 免费额度 1GB 出站/月，多人查询可能不够 |

---

## 功能设计

### 页面规划

**页面 1：大学信息浏览（替代 Qt Tab1）**
- 筛选条件：省份、城市、学校类型、办学模式、档次（985/211/双一流/强基）、名称模糊搜索
- 结果表格：排名、大学名称、省份、类型、档次标签
- 点击大学名 → 进入详情页

**页面 2：大学招生详情（替代 Qt Tab2）**
- 左侧：历年录取分数线（年份、批次/类型、最低分/位次、分差）
- 右侧：专业分数线（年份、专业、选科要求、最低分/位次）
- 支持表头筛选

**页面 3：专业录取查询（替代 Qt Tab3）**
- 按专业名称 + 年份查询所有高校该专业录取情况

**页面 4：招生计划查询（替代 Qt Tab4）**
- 按年份 + 学校 + 专业查询计划招生数、学费、学制、选科要求

**页面 5：定向志愿推荐（新增）**
- 输入：省份、分数、位次、选科（物理/历史 + 再选科目）
- 策略配置：冲/稳/保 阈值滑块
- 输出：院校+专业推荐列表，含历年分数、录取概率估算
- 支持勾选加入「我的志愿表」

**页面 6：志愿表管理（新增）**
- 查看已保存的志愿表
- 导出 Markdown / CSV

### 数据流

```
gaokao2025.sqlite（只读）
    │
    ▼
Flask 路由 → SQL SELECT → JSON / HTML
    │
    ▼
浏览器渲染（Jinja2 模板 / 简单前端）
```

---

## 代码结构

```
gaokao2025-web/
├── app.py              # Flask 入口，6 个路由
├── requirements.txt    # flask
├── gaokao2025.sqlite   # 软链接或复制到项目目录
├── templates/
│   ├── base.html       # 共用布局（导航栏）
│   ├── index.html      # 大学信息浏览
│   ├── school.html     # 大学详情
│   ├── major.html      # 专业查询
│   ├── plan.html       # 招生计划
│   ├── recommend.html  # 定向志愿推荐
│   └── wishlist.html   # 志愿表
└── static/
    └── style.css
```

---

## 部署步骤

### 1Panel 部署

```
1. 1Panel → 网站 → 创建 Python 项目
2. 上传 flask-web/ 目录 + gaokao2025.sqlite
3. 选择 Python 版本 → 启动命令 flask run --host=0.0.0.0 --port=5000
4. 1Panel 自动处理 Nginx 反向代理 + 域名 + HTTPS
5. 获得公网 URL → 发给同学
```

### 数据更新流程

```
每年 7-8 月：
1. 本地跑 Python 爬虫，生成 gaokao2025.sqlite
2. scp 到 VPS 覆盖旧文件
3. 重启 Flask 服务
```

---

## 与 Qt 应用的关系

| 对比 | Qt 桌面应用 | Web GUI |
|------|-----------|---------|
| 安装 | 需编译 + Qt 运行时 | 浏览器打开即用 |
| 分享 | 无法直接分享 | 发 URL 即可 |
| 功能 | 4 个 Tab 查询 | 6 个页面 + 志愿推荐 |
| 数据源 | 同 `.sqlite` | 同 `.sqlite` |
| 维护 | 需更新二进制 | 只需更新服务端代码 |

**建议**：Web 上线后，Qt 应用保留作为离线备选方案，不再继续开发新功能。

---

## 待办

- [ ] 等 `schoolscore` / `majorscore` / `college_plan` 爬取完成
- [ ] 评估数据库完整性和数据质量
- [ ] 确认是否保留 Qt 应用中的 Logo 校徽图片显示（Web 版本需用 `<img>` 标签渲染 Base64）
- [ ] 开始编写 `app.py` + 模板
- [ ] VPS 部署 + 验证
