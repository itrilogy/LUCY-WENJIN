# 爬虫调度器 + 碎片化设计备忘

## 现状（实际落地）

爬虫已改造为 **增量断点续爬** 模式，核心机制：

| 机制 | 实现方式 |
|------|---------|
| **断点续传** | `LEFT JOIN` 找 `schoolscore`/`majorscore`/`college_plan` 中缺失的学校，只爬缺失部分 |
| **负向缓存** | `crawl_skip` 表记录已确认无数据的 (学校, 年份, 课程类型, 表名)，下次跳过 |
| **防重写** | `INSERT OR IGNORE` + `INSERT OR REPLACE` + 唯一索引 |
| **网络容错** | 3 次超时重试（ReadTimeout → sleep 5s → 重试，3 次后放弃） |
| **事务安全** | `isolation_level=None` 自动提交 + `check_same_thread=False` 多线程兼容 |
| **自动建表** | `create_tables()` 带 `IF NOT EXISTS`，首次运行自动建 |

### 生产管道

```
college_info (3000 所)
    │
    ▼
schoolscore ─── 有数据 → 确认在赣招生（~2167 所）
    │                      │
    ▼                      ▼
 crawl_skip            majorscore（~1778 所有数据 + ~695 skip）
 (已确认无数据)             │
                          ▼
                     college_plan
                          │
                          ▼
                     Web GUI 查询
```

---

## 数据库表设计

### 实际 6 张表

| 表 | 用途 | 数据量 |
|-----|------|--------|
| `college_info` | 高校基本信息 | 3,000 所 |
| `schoolscore` | 录取分数线（学校级别） | ~21,000 行 |
| `majorscore` | 专业分数线（专业级别） | ~155,000 行 |
| `college_plan` | 招生计划 | 待爬 |
| `college_detail` | 学校简介 | ❌ 无数据（无爬虫） |
| `crawl_skip` | 无数据负向缓存 | 动态增长 |

### crawl_skip 表结构

```sql
CREATE TABLE crawl_skip (
    school_name TEXT,
    year TEXT,
    curriculum TEXT,
    table_name TEXT,          -- schoolscore / majorscore / college_plan
    checked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (school_name, year, curriculum, table_name)
);
```

---

## 爬虫函数详解

### 公共辅助函数

| 函数 | 作用 |
|------|------|
| `create_tables()` | 建 6 张表（IF NOT EXISTS） |
| `create_unique_indexes()` | 建 4 个唯一索引 + 调 `create_tables()` |
| `get_curriculum_list(year)` | 按年份返回科类列表（≥2024 → 物理类/历史类，≤2023 → 理科/文科） |
| `has_data(table, conditions)` | 查数据库是否有记录 |
| `is_skipped(school, year, cur, table)` | 查 `crawl_skip` 是否已标记 |
| `mark_skipped(school, year, cur, table)` | 写入 `crawl_skip` |

### insertSchool() — 学校信息

```
INSERT OR REPLACE INTO college_info(...)
LOGO: 下载 → Base64 → UPDATE（timeout=10，失败跳过）
```

### insertSchoolScore() — 录取分数线

```
LEFT JOIN 找 college_info 中缺失的学校
每个缺失的学校 → 5年 × 2科类 → getScore()
  → 有数据: INSERT OR IGNORE INTO schoolscore
  → 无数据: mark_skipped()  → 下次跳过
```

### getMajorScore() — 专业分数线（单线程）

```
LEFT JOIN 找缺失（排除 schoolscore 的 crawl_skip）
每个缺失的学校 → 5年 × 2科类 → getMajorScore()
  分页循环: pn=1,2,3... 直到 dataList 为空
  → 每页: INSERT OR IGNORE INTO majorscore
  → dataList 为空: mark_skipped()
  超时: 3 次重试 → 放弃
```

### getCollegePlan() — 招生计划

```
同 majorscore 逻辑
分页: rn=50, pn=0,50,100...
→ INSERT OR IGNORE INTO college_plan
```

---

## 使用方法

```bash
# 单校测试
python3 getdata2025.py test

# 全量爬取（按顺序跑）
python3 getdata2025.py school   # ~5分钟
python3 getdata2025.py score    # ~1-2小时
python3 getdata2025.py major    # ~1-2小时
python3 getdata2025.py plan     # ~1-2小时

# 增量更新（随时重跑，只补缺失）
python3 getdata2025.py score    # 已入库的跳过，只补新数据
```

---

## 经验教训

| 问题 | 修复 |
|------|------|
| 学校名被截断为单字 | `schoolName[0]` → `schoolName`（原来是 list of str, 改成 list of [tuple]） |
| SQLite 跨线程崩溃 | `check_same_thread=False` |
| 嵌套事务冲突 | `isolation_level=None` 自动提交模式 |
| 网络超时崩整个线程 | 3 次重试 + sleep(5) → 放弃 |
| LEFT JOIN 包含无 schoolscore 的学校 | 额外 `LEFT JOIN crawl_skip(schoolscore)` 排除 |
| 分页末页 KeyError | `mark_skipped` 放在 except 分支 |
| 学校清单无限循环 | `while True` → `while pageno <= MAX_PAGE` |

---

## 待做

- [ ] `scheduler.py` 调度器（`crawl_progress` 表 + cron 定时触发）
- [ ] `batch` 参数：每次只爬 N 所学校
- [ ] WebGUI 后台管理页面（触发爬虫、查看进度）
- [ ] 学校简介 `college_detail` 爬虫
